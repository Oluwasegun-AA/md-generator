const requiredAndAll = {
  required: true,
  optional: false,
  all: ['readme'],
}

const optionalAndAll = {
  required: false,
  optional: true,
  all: ['readme'],
}

export {
  requiredAndAll,
  optionalAndAll
};
